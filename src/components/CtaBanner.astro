<section class="cta-banner fade-in">
  <div class="container cta-inner">
    <h2 class="cta-title">Ready to understand your customers?</h2>
    <p class="cta-desc">Install RetainPulse and start segmenting today.</p>
    <a
      href="https://apps.shopify.com/retainpulse"
      class="cta-btn"
      target="_blank"
      rel="noopener noreferrer"
    >
      View on Shopify App Store →
    </a>
  </div>
</section>

<style>
  .cta-banner { padding: var(--section-py) 0; }

  .cta-inner {
    text-align: center;
    background: var(--color-white);
    border-radius: 16px;
    padding: clamp(2.5rem, 5vw, 4rem) 2rem;
  }

  .cta-title {
    font-size: clamp(1.5rem, 3vw, 2rem);
    font-weight: 600;
  }

  .cta-desc {
    margin-top: 0.75rem;
    color: var(--color-subtext);
    font-size: 1.0625rem;
  }

  .cta-btn {
    display: inline-block;
    margin-top: 1.5rem;
    padding: 0.875rem 2rem;
    min-height: 44px;
    background: var(--color-accent);
    color: var(--color-white);
    border-radius: 8px;
    font-weight: 600;
    transition: background 0.2s ease;
    cursor: pointer;
  }

  .cta-btn:hover { background: var(--color-accent-hover); }
</style>
